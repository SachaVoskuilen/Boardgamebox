import { Layout } from '@/components';
import { type NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <Layout loading={false} title="Home">
      <div>iets</div>
    </Layout>
  );
};

export default Home;
