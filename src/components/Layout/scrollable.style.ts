import { Box } from '@/styles';
import styled from 'styled-components';

export const Scrollable = styled(Box)`
  width: 100%;
  height: 100%;
  margin: 0 auto;
  position: relative;
  overflow-y: auto;
`;
