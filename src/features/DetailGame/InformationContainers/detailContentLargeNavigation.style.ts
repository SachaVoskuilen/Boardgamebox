import { Flex } from '@/styles';
import styled from 'styled-components';

export const StyledDetailContentLargeNavigation = styled(Flex)`
  flex-direction: row;
  justify-content: start;
  align-items: center;

  > * {
    padding: 5px 15px;
  }
`;
