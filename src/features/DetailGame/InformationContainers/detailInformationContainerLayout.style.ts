import { Grid } from '@/styles';
import styled from 'styled-components';

export const StyledDetailInformationContainerLayout = styled(Grid)`
  margin-top: 35px;
  grid-template-columns: 1fr;
`;

// ${({ theme }) => theme.sizes.navHeight} 200px
